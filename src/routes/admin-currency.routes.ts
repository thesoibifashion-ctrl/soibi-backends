import { Router } from 'express'; import { requireAuth, requireRole } from '../middleware/auth.js'; import { createAdminCurrency, deleteAdminCurrency, listAdminCurrencies, setAdminDefaultCurrency, updateAdminCurrency } from '../controllers/currency.controller.js';
const router = Router(); router.use(requireAuth); router.use(requireRole('admin', 'super_admin')); router.get('/', listAdminCurrencies); router.post('/', createAdminCurrency); router.patch('/:id', updateAdminCurrency); router.patch('/:id/default', setAdminDefaultCurrency); router.delete('/:id', deleteAdminCurrency); export default router;
